OPENPROJECT_BASE_URL=https://your-instance.example.com
OPENPROJECT_API_TOKEN=

# AI provider settings are configured in-app on the Connect screen (stored in
# SQLite + OS keychain, not read from .env). Documented here for reference only:
#   Groq (default):  base URL https://api.groq.com/openai/v1
#   OpenAI:          base URL https://api.openai.com/v1
